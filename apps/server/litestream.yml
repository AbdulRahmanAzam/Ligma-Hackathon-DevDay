dbs:
  - path: /var/lib/ligma/ligma.db
    replicas:
      - type: s3
        endpoint: ${LITESTREAM_R2_ENDPOINT}
        bucket: ${LITESTREAM_R2_BUCKET}
        path: ligma
        access-key-id: ${LITESTREAM_R2_KEY}
        secret-access-key: ${LITESTREAM_R2_SECRET}
        sync-interval: 1s
